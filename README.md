# College Event Management System

A full-stack event management system for colleges with professional legacy UI design.

## Tech Stack

**Frontend:**
- React 18
- React Router
- Tailwind CSS
- Lucide React Icons

**Backend:**
- Node.js
- Express
- CORS

## Project Structure

```
Project/
├── backend/              # Node.js Express server
│   ├── server.js        # Main server file with API routes
│   └── package.json     # Backend dependencies
├── src/                 # React frontend
│   ├── components/      # Reusable UI components
│   ├── contexts/        # React Context (Auth, Event)
│   ├── pages/           # Page components
│   ├── services/        # API service layer
│   └── index.css       # Global styles
└── package.json         # Frontend dependencies
```

## Installation

### Backend Setup

1. Navigate to the backend directory:
```bash
cd backend
```

2. Install dependencies:
```bash
npm install
```

3. Start the backend server:
```bash
npm start
```

The backend will run on `http://localhost:5000`

### Frontend Setup

1. From the project root, install frontend dependencies:
```bash
npm install
```

2. Start the frontend development server:
```bash
npm run dev
```

The frontend will run on `http://localhost:5173` (or another port specified by Vite)

## API Endpoints

### Authentication
- `POST /api/auth/admin/login` - Admin login
- `POST /api/auth/student/login` - Student login

### Events
- `GET /api/events` - Get all events
- `GET /api/events/:id` - Get event by ID
- `POST /api/events` - Create new event
- `PUT /api/events/:id` - Update event
- `DELETE /api/events/:id` - Delete event

### Registrations
- `GET /api/registrations?eventId=:id` - Get registrations for an event
- `GET /api/registrations/student/:studentId` - Get student's registrations
- `POST /api/registrations` - Create new registration
- `PUT /api/registrations/:id/attendance` - Update attendance status

### Certificates
- `GET /api/certificates/student/:studentId` - Get student's certificates
- `POST /api/certificates` - Generate new certificate

## Demo Credentials

### Admin
- Username: `sports_admin` / Password: `admin123`
- Username: `cultural_admin` / Password: `admin123`

### Student
- Email: `rahul@college.edu` / Password: `student123`
- Email: `priya@college.edu` / Password: `student123`

## Features

### Admin
- Create, edit, and delete events
- View and manage event participants
- Mark attendance via QR code simulation
- Update event winners
- Generate participation certificates

### Student
- Browse available events
- Register for events
- View registrations and attendance status
- Download certificates (when generated by admin)

## Design

The system uses a professional legacy design with:
- Navy/gray color palette
- Bordered components
- Table-based layouts
- Minimal rounded corners
- College branding throughout
